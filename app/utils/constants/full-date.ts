import { getDateStateForSelect } from '../helpers/getDateStateForSelect';

export const {
  days: DAYS,
  months: MONTHS,
  years: YEARS,
  educationYears: EDUCATION_YEARS
} = getDateStateForSelect();
